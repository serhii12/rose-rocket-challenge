import React from 'react';
import Leg from './Leg';

const LegsList = props => (
  // const renderList = props.map(data => <Leg />);
  <div className="legs col">LegsList</div>
);
export default LegsList;
